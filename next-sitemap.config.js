/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://gr-shop-2.vercel.app', // Your live domain
  generateRobotsTxt: true,
  // Optionally add custom paths or exclude private routes
}
